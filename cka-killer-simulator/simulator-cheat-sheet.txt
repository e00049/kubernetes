# create shortcuts in vim editor

~/.vimrc
set tabstop=2
set expandtab
set shiftwidth=2

# Enviroment variable:
export do="--dry-run=client -o yaml"

Question 1: Contexts:

You have access to multiple clusters from your main terminal through kubectl contexts. 
Write all those context names into /opt/course/1/contexts.

Next write a command to display the current context into /opt/course/1/context_default_kubectl.sh

Finally write a second command doing the same thingng into /opt/course/1/context_default_no?kubectl.sh, but without the use of kubectl.

Answers are:
# kubectl config get-contexts -o name > /opt/course/1/contexts
# echo "kubectl config current-context" > /opt/course/1/context_default_kubectl.sh
# echo "cat ~/.kube/config | grep current | sed -e 's/current-context: //' " > /opt/course/1/context_default_no_kubectl.sh


Question 10: RBAC ServiceAccount Role RoleBinding:

Create a new ServiceAccount processor in Namespace project-hamster. Create a Role and RoleBinding, both named processor as well.  These should allow the new SA to only create Secrets and ConfigMaps in that Namespace. 

# kubectl -n project-hamster create sa processor

# kubectl -n project-hamster create role processor --verb=create --resource=secrets --resource=configmap 

# kubectl -n project-hamster create rolebinding --role=processor --serivceaccount project-hamster=processor


Question 19: | Create Secret and mount into Pod

$ kubectl create namespace secret 

$ kubectl -n secret run secret-pod --image=busybox $do -- sh -c "sleep 5d" > 19-pod.yaml

$ kubectl -n secret create secret genaric secret2 --from-litral=user=user1 --from-literal=pass=123456 


