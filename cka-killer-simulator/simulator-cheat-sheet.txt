# create shortcuts in vim editor

~/.vimrc
set tabstop=2
set expandtab
set shiftwidth=2

# Enviroment variable:
export do="--dry-run=client -o yaml"

Question 1: Contexts:

You have access to multiple clusters from your main terminal through kubectl contexts. 
Write all those context names into /opt/course/1/contexts.

Next write a command to display the current context into /opt/course/1/context_default_kubectl.sh

Finally write a second command doing the same thingng into /opt/course/1/context_default_no?kubectl.sh, but without the use of kubectl.

Answers are:
# kubectl config get-contexts -o name > /opt/course/1/contexts
# echo "kubectl config current-context" > /opt/course/1/context_default_kubectl.sh
# echo "cat ~/.kube/config | grep current | sed -e 's/current-context: //' " > /opt/course/1/context_default_no_kubectl.sh



