# If we check the Pods we see two replicas:
e00049@kubernetes $ kubectl -n project-c13 get pod | grep o3db

# find respcetive statefull deployment of 03db pod's
e00049@kubernetes $ kubectl -n project-c13 get deploy,ds,sts | grep o3db

# To scale in 03db statefull deployment 
e00049@kubernetes $ kubectl -n project-c13 scale sts o3db --replicas 1
